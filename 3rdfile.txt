#include <stdio.h>
#include <stdlib.h>

int main()
{
    int n,k,i,z=0;

    scanf("%d %d",&n,&k);
    int a[n+1];
    for(i=0;i<n;i++)
    {
        scanf("%d",&a[i]);
    }
     for(i=0;i<n;i++)
    {
        printf("%d\n",a[k-1]);
        if(a[i]>=a[k-1]&&a[i]>0)
        {
            z=z+1;
        }


    }

    printf("%d\n",z);
    return 0;
}
